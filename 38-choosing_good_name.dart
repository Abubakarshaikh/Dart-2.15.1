void main() {
  // Every function is an action, so the name should contain at least one verb. For
  // * write(to: "filename.txt") means write to file
  // * reloadTableData() means to reload table data
  // * isBirthDay() means checking if today is birth day
  // * getAddress() or setAddress("My Address") mean getting or setting an object's address.
  // Calculate percentage

  // * Use noun phrases for pure functions;
  //  - averageTemperature instead of getAverageTemperature
  //  - studentNames instead of extractStudentNames

  // * Use verb phrases for functions with side effects.
  //  - updateDatabase or printHello

  // * Also use verb phases if you want to emphasize that the function does a lot of work
  //  - calculateFibonacci or parseJson

  // * Don’t repeat parameter names in the function name.
  //  - e cube(int number) instead of cubeNumber(int number)
  //  - printStudent(Stringname) instead of printStudentName(String name)
}
